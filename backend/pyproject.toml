[project]
name = "castle-sec-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "castle-sec-game",
    "fastapi[standard]>=0.129.1",
    "pydantic>=2.12.5",
]

[tool.uv.sources]
castle-sec-game = { path = "../game", editable = true }

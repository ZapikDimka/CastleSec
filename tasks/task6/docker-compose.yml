version: '3.8'

services:
  ctf-guard:
    build: ./app
    container_name: task6
    ports:
      - "8080:8080"  # Змінено тут
    environment:
      - PORT=8080    # Змінено тут
    restart: unless-stopped
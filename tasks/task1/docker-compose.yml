version: '3.8'

services:
  task1:
    build: .
    image: castlesec-task1:latest
    ports:
      - "8080:80"
    networks:
      - castle_network
    restart: unless-stopped

networks:
  castle_network:
    driver: bridge
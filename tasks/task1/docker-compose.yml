version: '3.8'

services:
  task1:
    build: .
    container_name: castle_task1_recon
    ports:
      - "8080:80"
    networks:
      - castle_network
    restart: unless-stopped

networks:
  castle_network:
    driver: bridge